import React from 'react';
import { Row, Col } from '@d4sd/components';
import './style.less';

const WorkspaceLayout = (props: any) => (
  <div>
    <h1>Welcome to D4SD</h1>
    {/* <button onClick={()=> app.auth().signOut()}>Logout</button> */}
  </div>
);

export default WorkspaceLayout;
